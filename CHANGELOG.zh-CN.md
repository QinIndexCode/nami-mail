## [Unreleased]

## [0.4.1] - 2026-09-25

### 新增

- BIMI 发件人品牌图标支持：解析与验证企业官方发件人 SVG 品牌图标，增强官方验证邮件的视觉辨识度；具备严格的 SVG 安全净化、尺寸规整、内存与磁盘双级缓存及 TTL 过期保护机制。
- 写信窗口附件拖拽与剪贴板截图粘贴：写信对话框支持将本地文件直接拖拽添加为附件，具备清晰的虚线投放反馈遮罩；正文编辑区支持剪贴板图片 `Ctrl+V` 直接粘贴上传，显著提升截图发信效率。

### 修复

- 重构 Agent 敏感操作审批确认卡片与输入框吸附衔接：在 `.agent-composer` 应用实体背景（`var(--panel-solid)`）作为物理遮挡层，配合外层插槽的 `-14px` 负边距抽屉插槽咬合，使卡片底边缘自然插入输入框后方；彻底去除原底边渐变蒙版导致的悬浮断层虚边，移除卡片底部边框；扩充卡片下部内衬（24px 安全缓冲）杜绝遮挡决策按钮，并在展开与折叠全周期应用同步的贝塞尔缓动，决裁后复位零位移跳变。
- 阅读器同语言邮件翻译提示抑制：智能探测邮件正文语言特征，当语言与客户端当前界面语言一致时，默认不显示多余的“翻译为当前语言”横幅，消除标题与正文间的视觉干扰。
- 修复 Windows CI 桌面端冒烟测试中的窗口冷启动超时误报：放宽 GitHub Actions 运行器的窗口加载基线时间，避免高负载运行器下的偶发抖动。

### 文档

- 新增 `v0.4.1` 中英文发布说明；官网与 Issue 模板版本号同步更新至 0.4.1。

## [0.4.0] - 2026-09-21

### 新增

- 静态官网与文档站：落地页与从 `docs/` 构建的文档页（GitHub Pages 自动部署），支持中英双语切换。
- 会话线程补全：跨目录的 Gmail 式会话归组。消息记录没有线程列，线程先由客户端对已加载集合归组，缺失的成员再通过新端点 `GET /api/messages/:id/thread` 补全（账户内按 RFC 头做双向传递闭包，排除草稿，逐行解密时让出事件循环）；发送后自动补取线程。
- 引用自动折叠：HTML 邮件顶层引用块折叠为可点击展开的原生 `<details>`，纯文本折叠尾部 `> ` 引用块；折叠只发生在渲染层，净化、翻译与回复引用管线仍使用完整原文。

### 修复

- 修复 Agent 撤回后立即重发的一连串竞态：会话回合持久化后服务端现在立即释放会话槽——此前收尾的首回合标题生成（慢供应商下最长约两分钟）会一直占住槽位，期间撤回后重发全部被 CONFLICT 拒绝，乐观消息从未落库；再次撤回报"消息不存在"，界面出现两条重复消息，只有重新进入会话才恢复。前端撤回遇"消息不存在"时改为采纳服务器快照对账（幽灵行立即消失，无需重进会话），输入框保留待编辑重发的文本。
- 修复新邮件提示音"通知弹出但无声"的问题：主进程改为先播放、后弹横幅——播放成功才静音系统音，任何失败自动回退为带系统音的通知并把原因写入运行日志；应用启动后预热播放管线，消除首次通知约 1.5 秒的冷启动延迟；设置页"测试通知／测试提示音"改走与真实新邮件完全相同的管线，测试听到的就是真实通知的效果。
- 修复切换账户、文件夹或视图时的列表闪烁：列表标识只跟随已落地的快照身份，请求进行中旧行仅整体变暗，不再整列重建。
- 修复自己发送邮件的方向展示：现在按收件人渲染（阅读区头像与主名显示首个收件人，列表显示 To 行与收件人头像，快捷回复预填原收件人），不再按文件夹名猜测方向；收件方向维持发件人展示。
- 修复 Agent 网络搜索工具的解析脆弱性：不再依赖搜索结果页的 class 名（其变更会导致永远返回 0 条结果），改为两步解析属性与链接；检索活动现在展示真实查询词与结果概要，替代通用状态文案（旧记录向后兼容）。

### 改进

- 设置页信息架构精简：删除 21 条冗余副标题、精简 11 条（中英同步）；下拉框未展开时的字号与展开菜单一致；新增设置页本地化交互测试。
- 更新提示改为左下角圆形徽章：悬停展开为"发现新版本 x.y.z"胶囊（含下载与关闭），关闭时分层动画退出；左下角主文案由"本地加密"改为"数据留在本机"。
- 账户文件夹图标语义调整：垃圾邮件改为盾牌加感叹号、所有邮件改为层叠样式，并修复图标的垂直对齐。
- 依赖升级：imapflow 1.7.8 → 2.0.2（TypeScript 化；上游修复了 IDLE 会话自行结束后不重入的问题）、react 19.3.0（含 react-dom 同步）、lucide-react 1.45.0、@tanstack/react-virtual 3.14.12。

### 文档

- 新增 `v0.4.0` 中英文发布说明；官网版本号同步。

## [0.3.0] - 2026-09-13

### 新增

- 引入首个正式的 External Mail v1 本机接口（仅 Windows 桌面版）：面向已配对的本地脚本与 MCP 客户端，保持本地优先、默认拒绝和只读边界；
- 新增安装包提供的 `namimail` PATH shim，并支持直接以 `"Nami Mail.exe" --cli <args>` 调用；
- 新增 `namimail service start`、`status`、`doctor`，用于显式启动或检查本机 AgentHost；普通读取命令与 MCP stdio 不会隐式启动宿主；
- 新增桌面可见确认的 `namimail pair --profile <name>` 与 `namimail revoke --profile <name>` 流程；首次配对固定当前账户 ID 快照，后续新增的账户不会自动进入旧 profile；
- 新增 `namimail mcp start --profile <name>` 本机 MCP stdio 接入：支持 MCP `2025-03-26` 与 `2025-06-18` 两种协议（服务端回显客户端所请求的版本），`tools/list` 发布 16 个 External Mail v1 工具（9 个只读 + 7 个受限写入）；
- External Mail v1 提供账户、文件夹、邮件列表、邮件详情（单个与批量）、线程与附件元数据的读取，以及受限写入：草稿创建/更新/删除、邮件移动（归档/废纸篓）、已读/星标设置、发送与回复；成功数据由版本化严格 schema 验证，邮件正文为受限纯文本，附件不会导出为文件；
- 新增邮件模板库：可在设置中管理常用主题与正文模板，撰写或回复邮件时一键插入；
- 新增附件内联预览：PDF 与图片在只读预览中渲染，DOCX / PPTX / XLSX 与常见文本文件提供只读文本预览，不导出原始文件。
- 桌面端新增运行日志 `runtime-log.jsonl`：未捕获异常、渲染进程异常退出与本地服务错误会落盘（受 `NAMI_MAIL_NO_STARTUP_LOG=1` 总开关控制），反馈问题时可随附。
- 自动回复配置失效（如缺少 API Key）时，设置页给出明确警告而不是静默失败。
- 外部 MCP / CLI 的 `messages move` 现在可以移动到同一账户下的**任意文件夹**（MCP 的 `folder` 参数、CLI 的 `--folder`），不再局限于归档与废纸篓这两个快捷目标；`target` 与 `folder` 二选一，权限档位与确认流程不变。
- 外部 MCP / CLI 新增 **全文检索** `messages search`（MCP `namimail_messages_search`）：可按账户、文件夹、主题、有无附件、时间范围与游标翻页检索本地邮件（主题、发件人、收件人、附件名与正文），每条返回以关键词为中心的有界摘要片段。结果同时报告 `searchedFrom`（未指定时间范围时默认只搜最近约 90 天）与 `newestLocalAt`（本地同步进度），便于区分「没有匹配」与「尚未同步」；一至两个字符的中文关键词此前会静默搜不到，现已改为回退子串匹配。

### 改进

- 为脚本与 MCP 提供稳定的成功/失败包络、错误码、账户范围与更新排空恢复语义；GUI 主实例在命名管道 Broker 异常退出后主动重建，避免"已运行但不可用"的假成功；
- CLI/MCP 仅通过已配对、仅当前 Windows 用户 SID 可访问的命名管道 Broker 访问正在运行的宿主；不提供 Fastify token、HTTP/TCP、SQLite、文件 URI、邮箱凭据或数据库路径作为集成通道；
- 保留实验性本地 NLLB-200 翻译，仍仅在桌面阅读器中由用户主动准备和触发，不作为 CLI/MCP 工具。
- 日历色板与附件图标降饱和，与语义色的灰调风格一致；小字标签在其底色上的对比度从最低 2.2:1 提升到 ≥4.5:1（满足无障碍 AA），深色主题同步调整。
- 浮层与卡片的阴影统一到设计 token（菜单/气泡/筛选面板一层、卡片与状态条一层），深浅色主题自动切换一致，浅色主题下弹出的菜单不再比同类浮层"更飘"。
- 性能：统计接口合并为单次扫描、同步按最新邮件优先、RAG 回填批量化、滚动揭示改事件委托。
- 本地邮件服务改为在独立进程中运行：SQLite 同步、IMAP 收发与解密不再与窗口共用同一个事件循环，邮箱同步或批量操作时界面不再冻结。
- 键盘可达性：附件预览关闭后焦点还原、邮件标题焦点环、斜杠/提及菜单与收件人建议的键盘导航、Shift+J/K 范围多选。
- 启动日志与运行日志按固定行数裁剪，不再无限增长。
- Agent 会话生命周期改为状态机实现，后台缓冲与重放更稳定。
- 切换账户、文件夹、视图或搜索条件时不再用六行骨架屏替换整个列表：当前邮件会保留到新列表到达，并在到达时淡入，列表不再变矮、布局不再跳动，阅读位置也不会丢失。
- 桌面端的测试脚本显式登记并纳入 CI。

### 文档

- 新增 `v0.3.0` 中英文发布说明，并如实标注：从 `0.2.3` 到 `0.3.0` 的公共网络自动更新验收仍待完成，`0.3.0` Windows 安装程序未做 Authenticode 签名。

### 变更

- 阅读区宽度改为**填充整个阅读列**（与 Gmail 一致），不再被 `--measure` 卡住：此前 672px 的居中列会把邮件作者写定的 600px 宽表格挤进更窄的容器，配合单元格的 `overflow-wrap:anywhere` 就把词从中间折断——Apple 的推广邮件等 HTML 邮件因此出现异常换行。现在只有**没有自带排版的纯文本正文**保留行长上限（960px），且仍从列的内边距左缘开始，与主题左缘对齐。
- 本地邮件检索的第二路从「向量语义检索」改为「**查询扩展**」：词法检索没有结果时，用当前默认模型把问题扩展成同义词、领域词与中英对照词，再查同一份本地索引。由此**不再需要配置嵌入模型**（设置页的「嵌入模型」字段已移除），也不再存在任何向量索引与冷启动重建成本；**只有用户的问题文本会发给模型，邮件正文与片段始终留在本机**。扩展调用按情形限时（词法无结果时 10 秒、仅低分召回时 800ms），失败或超时都等同于“没有额外检索词”，检索退回纯词法、不会中断；同一问句的扩展词有界缓存。`GET /api/agent/rag/verify` 的一致性报告新增 `expansion` 计数（触发 / 救回 / 未救回），用于判断这条路的实际收益。

### 修复

- 修复首次运行时"翻译服务条款"对话框可能被通知/更新提示遮住、窄窗口下按钮点不动的问题：现在该对话框会被识别为打开的模态，提示层自动退到其后（更新提示也改为等它关闭后再出现）。
- 修复远程图片代理可被重定向引导访问内网/本机地址的问题：现在逐跳校验目标、拒绝私有地址解析，并新增该模块的安全测试。
- 修复数据库迁移失败可能留下半迁移状态的问题：迁移以事务执行，失败时释放连接，不再锁死数据库文件。
- 修复应用关闭时，发件投递确认后台任务可能继续访问已关闭本地服务的问题。
- 修复 Agent 快捷键在翻译词条、附件预览弹窗打开时被误触发的问题；本地服务超时现在返回独立错误码。
- 修复一次 Agent 往返后邮件列表滚动位置未恢复的问题。
- 修复邮箱正在同步时，移动到废纸篓／归档等操作会立刻失败的问题：现在会先等正在进行的同步结束（最长约 15 秒）再执行；若仍未轮到，会明确提示"邮箱正在同步，请稍后重试"，而不是给出与该邮件无关的移动状态说明。
- 修复连续操作时乐观状态被回滚的问题：标记已读／删除／星标后，另一次操作完成所触发的列表刷新会把仍在排队中的那几封改回操作前的样子（已读翻回未读、删掉的邮件重新出现、星标被撤销）。现在所有乐观变更都会统一登记，任何刷新路径（含分页加载）都不会覆盖它们。
- 修复批量操作结束时的列表跳转：批量任务运行期间若切换到别的文件夹，任务结束后列表会被拉回任务开始时所在的文件夹。
- 修复 Agent 会话之间的邮件引用串味：切换会话时不再把上一会话引入的引用带入新会话（这些引用会随消息一起持久化，此前会把用户从未在该会话引用过的邮件写进记录）。
- 修复设置页连续修改两项设置时，先发出的保存回包把刚改的选项改回去的问题。
- 批量操作（标记已读／未读、加星等）完成后自动退出多选状态，不再让已处理的选中项继续处于选中而误触发第二次批量操作；操作失败时保留选择以便重试。
- 修复发送记录的两处状态竞态：已取消的定时邮件不再被稍晚返回的列表响应重新加回；已确认发送的记录不会退回"发送中"。
- 修复侧栏未读数在标记已读后跳回旧值：列表与统计来自同一份服务端快照，此前只修正了列表行。
- 修复联系人、模板、日历里删除条目后被更早发出的缓存结果"复活"的问题。
- 修复设置弹窗内正在填写的内容被外部设置变更覆盖（未保存的翻译地址、回复模板等不再丢失）、桌面更新进度可能回退、以及 Agent 会话切回时读取记录失败但后台仍在生成回复却显示为空闲的问题。
- 自动回复决策：搜索改为输入停顿后再查询，切换筛选条件时不再显示上一次筛选的结果。
- Agent 服务商 / MCP 服务器设置：列表刷新不再清空正在填写的表单（含已输入的 API Key）；审批待回复后的刷新不再被进行中的轮询丢弃。
- 未读视图：刚读的邮件保留在列表中时，"N 封未读"不再把它重复计入，"全选匹配"也不会超过服务端实际能处理的范围。
- 桌面新邮件：通知订阅不再被反复重建，刷新期间到达的邮件不再漏掉提示与吐司。
- Agent：流式回复不再被稍早的服务端快照回退——切回会话、后台补收轮询现在按「保留更完整的一份」合并；被服务端拒绝的重试不再把上一次尝试的残留片段带到新回复上。
- Agent：点击「停止」立即收起生成状态（不再等服务端确认）；重命名会话即时生效，失败自动还原。

## [0.2.3] - 2026-07-28

### 新增

- 将此前仅存在于未公开源码标签中的实验性 NamiMail Agent 工作区纳入 `0.2.3` 待发布版本：可配置 OpenAI-compatible 或 Ollama Provider，在明确账户范围内使用本地词法检索与来源引用；
- Agent 仍仅可读取账户、文件夹、邮件、线程和附件元数据，以及创建、编辑或删除草稿；高风险邮件操作继续保留在主邮件界面。

### 改进

- 延续多账户侧栏、主题化设置控件和实验性主动邮件翻译的用户体验改进；
- 强化 Windows package、installer 和 desktop smoke 的分层诊断；安装器 smoke 的临时已安装应用验证改为受控、项目拥有的隔离位置，并在成功后清理，避免系统临时目录环境差异掩盖实际失败。

### 文档

- 新增 `v0.2.3` 中英文待发布说明，并将 `v0.2.2`、`v0.2.1`、`v0.2.0` 准确标记为没有公开 Release 或更新资源的源码标签；
- `0.2.3` 不把本地构建、CI 或资源生成表述为已完成真实线上自动更新验收。

## [0.2.2] - 2026-07-28（未公开发布的源码标签）

`v0.2.2` 的 Windows Release 流程在创建草稿或公开 GitHub Release 前停止，没有公开 Release、Windows 安装程序或更新资源，不能作为安装或自动更新来源；其用户可见功能和发布修复由 `0.2.3` 待发布版本承接。

### 新增

- 延续此前未公开源码标签中的实验性 NamiMail Agent 工作区：可配置 OpenAI-compatible 或 Ollama Provider，在明确账户范围内使用本地词法检索与来源引用；
- Agent 仍仅可读取账户、文件夹、邮件、线程和附件元数据，以及创建、编辑或删除草稿；高风险邮件操作继续保留在主邮件界面。

### 改进

- 延续多账户侧栏、主题化设置控件和实验性主动邮件翻译的用户体验改进；
- 将 Windows package、installer 和 desktop smoke 的监督预算分层，保留冷启动失败时可操作的桌面诊断，而不是由父进程抢先终止。

### 文档

- 保留 `v0.2.2` 中英文说明作为未公开源码标签的历史记录；
- 本标签不把本地构建、CI 或资源生成表述为已完成真实线上自动更新验收。

## [0.2.1] - 2026-07-28（未公开发布的源码标签）

`v0.2.1` 只有源码标签，未生成公开 GitHub Release、Windows 安装程序或更新资源，不能作为安装或自动更新来源；其用户可见变更经 `0.2.2` 未公开源码标签延续，并由 `0.2.3` 待发布版本承接。

### 新增

- 新增实验性的嵌入式 NamiMail Agent 工作区，可配置 OpenAI-compatible 或 Ollama Provider，并在明确账户范围内使用本地词法检索与来源引用；
- Agent 可读取账户、文件夹、邮件、线程和附件元数据，并创建、编辑或删除草稿；Provider 设置可从 Agent 与设置界面进入。

### 改进

- 优化侧栏导航：全部账户使用独立的账户聚合图标，发送状态使用独立状态图标，系统文件夹按实际用途显示对应图标；
- 强化账户选中、悬停和键盘焦点状态，避免左侧强调条；账户与文件夹区域可在空间受限时独立滚动，保留设置和本地加密状态入口；
- 收紧 Agent/RAG 公开边界说明，明确当前词法检索、附件正文摄取和外部接口的实际状态。

### 修复

- 修复 Windows 发布 smoke 的嵌套超时竞争，保留冷启动失败时可操作的桌面诊断；
- 修复设置内已展开主题下拉框按 `Esc` 时可能连同设置窗口一起关闭的问题。

### 文档

- 保留 [v0.2.1 Release Notes](docs/releases/v0.2.1.zh-CN.md) 作为未公开源码标签的历史记录，并同步维护英文版本；
- 将发布下载说明改为以 Release 页面列出的 Windows `.exe` 安装程序为准，避免将本地构建文件名误当成远端资源显示名。

## [0.2.0] - 2026-07-28（未公开发布）

`v0.2.0` 仅保留为未完成发布验证的源码标签，未生成公开 GitHub Release 或更新资源，不能作为安装或自动更新来源；其用户可见变更经 `0.2.1`、`0.2.2` 源码标签延续，并由 `0.2.3` 待发布版本承接。

## [0.1.2] - 2026-07-24

### 修复

- 修复 Windows checkout 的换行符差异会误判生成语言目录过期的问题；
- 为执行 SQLite `VACUUM` 和 WAL 实体检查的两条安全迁移测试设置明确且受限的 30 秒上限，避免受限 Windows runner 上的错误超时。

## [0.1.1] - 2026-07-24

### 新增

- 新增按当前界面语言主动触发的邮件正文机器翻译；结果可隐藏、重新显示和复制，并明确提示机器翻译可能不准确；
- 新增翻译服务设置：兼容 LibreTranslate，支持本机加密保存服务地址、超时和可选 API Key；保存要求有效地址，移除配置需要明确确认，且只在用户主动翻译时发送正文；
- 新增 JSON 界面语言包、本地化校验与中英文公开文档维护规则。

### 改进

- 改进收件箱、归档、发送状态和设置中的状态呈现，减少阅读未读邮件时不必要的列表跳变；
- 改进账户接入、邮件正文复制、验证码提取和网络/TLS 错误提示，使常见收发问题更容易处理；
- 统一菜单、下拉框、提示和弹窗的主题表现，弹窗使用半透明遮罩而非背景模糊。

### 文档

- 补充从 GitHub Release 安装、首次启动、更新选择、卸载与 Windows SmartScreen 风险的用户指南；
- 补充常见邮箱服务商的认证准备、OAuth 边界、手动 IMAP/SMTP 和排障指南。
- 新增可直接用于发布的 [v0.1.1 Release Notes](docs/releases/v0.1.1.zh-CN.md)；说明中明确区分已发布资源与尚待真实线上验证的自动更新路径。
- 将安全策略和 GitHub Issue 联系入口固定为已启用的 GitHub 私密漏洞通报路径，不再指向未配置的联系方式。

## [0.1.0] - 2026-07-22

- 本地优先的多账户 IMAP/SMTP 邮箱聚合、阅读、草稿、发送和同步；
- Google 与 Microsoft 公共客户端 OAuth，以及常见邮箱的应用专用密码/授权码引导；
- Windows 桌面版、本地数据加密与 GitHub Release ZIP 更新基础设施；
- 启动检查、更新/跳过/稍后提醒、ZIP 完整性复核和更新后缓存清理；
- 开源贡献、安全、隐私、支持、开发和发布文档。

面向用户的首发说明见 [v0.1.0 Release Notes](docs/releases/v0.1.0.zh-CN.md)。

该版本的 Release 资源与本地更新链路需要分别完成真实网络验证；在旧版到新版的公开线上升级验收完成前，不应将本条目视为自动更新的线上验证结论。
