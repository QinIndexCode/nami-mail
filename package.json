{
  "name": "nami-mail",
  "version": "0.1.0",
  "private": true,
  "description": "A local-first, multi-account web mail client.",
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -k -n api,web -c blue,magenta \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm --workspace @nami/server run dev",
    "dev:web": "npm --workspace @nami/web run dev",
    "build": "npm --workspace @nami/web run build && npm --workspace @nami/server run build",
    "start": "npm --workspace @nami/server run start",
    "test": "npm --workspace @nami/server run test",
    "typecheck": "npm --workspace @nami/server run typecheck && npm --workspace @nami/web run typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=22"
  }
}
