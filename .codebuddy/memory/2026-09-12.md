# 2026-09-12

## 全仓完善度体检（用户：「先检查一下完善程度再决定」）

### 门禁实测（本日）
| 项 | 结果 |
|---|---|
| 四 workspace typecheck | 全绿 |
| web 单测 | 699/699 |
| desktop 单测 | 193/193 |
| server 单测 | 800（798 通过 / 2 失败） |
| e2e | 17/17 |
| `npm audit --omit=dev --audit-level=high` | 0 |
| `smoke:desktop`（热跑） | 绿，local-service-ready 2212ms / window-loaded 2500ms |

### 关键结论
1. **唯一红色 = `app.test.ts` 的 2 条**，且已定位为**测试过期**（详见 MEMORY「验证命令备忘」）：默认值三处一致为 none/80，测试写成 coast/68；`backgroundIntensity` 上限已放宽到 100，测试仍用 81 当非法值。修测试即可全绿，**零风险**。
2. **smoke:desktop 曾报 4549ms 超 3000ms 基线**——是 `npm run smoke:desktop` 先跑全量 build 后的冷缓存/机器负载所致；跳过 build 直接 `node scripts/smoke-desktop.mjs` 复跑为 2500ms。**以后判断启动回退要跳过 build 再跑**，否则会误判。
3. 行数复测与旧记录有出入，以本次为准：`App.tsx` 3930、`main.mts` 2092、`agent-service.ts` 3072、`styles.css` 16933。

### 状态
- 工作树干净，仅 `.codebuddy/` 未跟踪；`tmp-pw-out*/` 已 gitignore（**目录仍在磁盘，待用户确认删除**）。
- **main 领先 origin 21 个提交（其中 15 个是本轮成果），未推送**；而交付流程约定「绝不 push main」、交付分支是 `backup/backgrounds-baseline` —— 这是当前最大的**流程风险**：所有成果都堆在不该推的分支上，需要决定是迁到交付分支、还是修订分支约定。
- 大文件拆分：计划（`docs/REFACTORING-PLAN.*`）与接缝测绘已完成，**代码一行未动**。

### 本轮未做（等用户决定）
- 修 `app.test.ts` 两处过期期望（5 分钟、零风险，改完全仓全绿）
- 大文件拆分第一步（托盘，约 −110 行 / 8 处接线）
- `tmp-pw-out*` 删除
- 提交归属分支的问题

## 用户拍板后执行（「大文件一定要拆分，但不要破坏功能」）

### 1. 修掉唯一红色（`9cc7438`）
`app.test.ts` 两处期望改为 `none`/80，非法样例 `81` → `101`。**server 800/800 全绿**，全仓不再有任何失败用例。

### 2. 大文件拆分第 1 步：托盘（`7db4dc2`）
新建 `apps/desktop/src/tray.mts`：`createTrayController({ getMainWindow, getAppIcon, loadAppIcon, copy, showError })` → `{ ensure, destroy, getTray, focusWindow, hideWindowToTray, applyBadge, setWindowVisible }`。
- `tray`、`trayBadgeIcon`、`mainWindowVisible` 收进模块；`appIcon` 留在 main（窗口外壳也要用）通过 getter 注入。
- main.mts **净 −136 行**（+33 / −169）。
- 接线点全部改完：smoke 桥、全局快捷键、关闭流程 ×2、`applyBadge` ×2、`hideWindowToTray` ×2、窗口 focus 事件、窗口外壳的 visibility。
- **验证（功能未破坏）**：desktop 双配置 typecheck 绿；desktop 193/193；四 workspace typecheck 绿；真实 Electron 冒烟无断言失败，3 个 `trayCreated` 探针为 true、`gracefulExit=true`、window-loaded 2419ms。
- 踩坑记：① 托盘函数与非托盘代码交错，必须按函数搬；② 控制器必须在 `nativeCopy` 之后创建（依赖注入）；③ 冒烟输出里 `trayCreated` 有 4 处，其中 1 处 false 是未触发区段的初始值，别误判。

### 剩余
大文件拆分第 2-4 步（配对 / 窗口外壳 / 关闭流程）、`App.tsx` 与 `styles.css` 测绘；`tmp-pw-out*` 删除；提交分支归属（main 已 ahead 23，按约定不该推 main）。

## 第二轮（同日）：搁置配对 + 清理冗余 + 文档语言后缀

### 1. Agent 配对 → futurePlan（`d4caa50`）
用户拍板暂不实施。`docs/REFACTORING-PLAN.zh-CN.md` / `.en.md` 里该节标题加「已搁置，列入 futurePlan」并写明当前顺序（托盘 → 窗口外壳 → 关闭流程），**测绘结果与接线点保留**，以后可直接开工。

### 2. 清理冗余与日志
已删除（全部是 gitignore 的，仓库零影响）：
- 仓库内 **所有 `*.log`（剩余 0）**，含 `output/`（约 70 个构建/冒烟日志）、`scripts/*.log`、`.playwright-cli/`、`.playwright-mcp/`（含 604KB / 448KB 的大日志）、`apps/server/*.log`、`apps/web/*.log`、根 `tmp-*.log`、`data/ui-stress/`。
- `artifacts/`、`gui-test-screenshots/`、`.zcode/`。
**未删（被 bulk-delete 阈值 500 拦下，需用户确认）**：`tmp-pw-out`（599 项）、`test-results`（984 项）。
保留：`data/`（可能有开发数据库）、`node_modules/`、`dist/`、`release-artifacts/`、`.mimosa/`。

### 3. 文档语言后缀统一（`facabe0`）
- 根 `CHANGELOG.md`/`CONTRIBUTING.md`/`CODE_OF_CONDUCT.md`/`SECURITY.md`/`SUPPORT.md` + `docs/releases/README.md` → 改为**纯语言入口**。
- `ARCHITECTURE-ROADMAP.md` → `ARCHITECTURE-ROADMAP.zh-CN.md`（无英文版、无 md 引用，重命名安全）。
- 两个 archive 进度文档加 `.zh-CN` 后缀（`docs/development/*.archive.zh-CN.md`，均未被引用）。
- 规则写入 `docs/DEVELOPMENT.zh-CN.md` / `.en.md` 新增「文档语言与命名」小节。
- 复查：现存无后缀 md 已全部为入口文件。

### 待办
`tmp-pw-out` / `test-results` 删除确认；窗口外壳拆分；分支归属。

## 第三轮（同日）：文档配图重做为运行态截图（`38a3f48`）

用户要求：md 里的图片/运行案例截图可能过期且**绝不含敏感数据**，要用**模拟数据**重做，且必须是**运行态截图**。

### 做法
- 新增 `e2e/shots.spec.ts`：对运行中的 demo 应用（`?demo=1`，`colorScheme: light`）截 4 张——收件箱/Agent 工作区 × 中英，写回 `docs/nami-mail-inbox-{zh-CN,en}.png`、`docs/nami-mail-agent-{zh-CN,en}.png`。
- 语言切换：`page.addInitScript` 预置 `localStorage["nami-mail.locale-preference"]`（demo 不落盘，这是唯一开关）。
- **脚本默认 `test.skip`**，只有 `NAMI_MAIL_CAPTURE_SHOTS=1` 才跑，避免 CI/常规 e2e 重写已提交图片。
- 删除未被引用的 `docs/nami-mail-inbox.png`。
- 验证：`npx playwright test`（4 个 spec 显式列表）仍 **17/17**；已肉眼核对 4 张图，内容全部来自 demo 数据集（`example.com`、虚构发件人 North Bank / 林游 / Mira Studio / Noah）。

### 发现
1. **列表中部那条横向浅带不是缺陷**：是 demo 的 `coast` 背景预设（海岸线）。`elementFromPoint(700, 320/360/400)` 探针确认该处只有 `.message-subject`/`.message-tags` 等正常行。
2. **真实 i18n 缺口**：`agent/agent-demo-data.ts` 的 `createDemoConversation()` 硬编码中文，英文界面下 Agent demo 会话内容仍是中文（`demo.ts` 的邮件数据是按 locale 生成的，只有这个会话没有）。
3. **e2e 全量跑会挂**：`e2e/ui-stress.spec.ts` 依赖 `scripts/ui-stress/run-stress.mjs` 的种子清单，必须用 4 spec 显式列表。
4. 敏感数据扫描：已跟踪文件只有 `.env.example`（值全空）与两个名为 `account-credentials.ts` 的源码；无 `.pem/.key/.db`；文档正文无邮箱地址。**从未 push 过任何提交**（main 领先 origin 26 个，全部在本地）。

## 第四轮（同日）：默认无背景 + 修 demo 会话 i18n（`3657b07`）

用户要求：软件默认使用无背景（纯色），截图同样；并修 i18n 缺口。

### 改动
1. **demo 模式不再强制 `coast` 预设**（`App.tsx`）。真实默认本来就是 `none`（`types.ts:412` 与服务端 `settings.ts:72-73` 一致），只有 demo 把它改成 `coast`——等于用样例帧展示了新安装永远不会出现的界面。现在 demo 也用纯色底。
2. **`createDemoConversation(locale)`**：demo 会话按界面语言生成。原来文案硬编码中文（`demo.ts` 的账户/邮件/投递数据都是按 locale 生成的，**只有这个会话漏了**），英文首次打开 Agent 面板看到的是中文。实现方式：函数内 `english = locale.startsWith("en")` + `tr(zh, en)` 内联成对文案；**ID、工具名、状态、结构一律未动，只翻译文案**。
3. 用新脚本重新截图：4 张图体积明显下降（agent 188KB→88KB、inbox 239KB→131KB），因为底图从海岸插画换成了纯色。

### 门禁
web typecheck 绿；web 单测 **699/699**；标准 4 套 e2e **17/17**（冒烟用例仍断言中文 demo 会话——它不预置 locale，走默认 zh-CN，所以不受影响）。

## 第五轮（同日）：暗色截图 + 删除临时目录 + 首次推送

### 1. 截图改暗色（`6f8edab`）
`e2e/shots.spec.ts` 的 `colorScheme` 由 light 改 **dark**（用户指定：暗色是展示主题），重截 4 张并肉眼核对——暗色 + 纯色底 + demo 模拟数据 ✓。

### 2. 删除用户确认的临时目录
`tmp-pw-out`（599 项）、`tmp-pw-out2`、`test-results`（984 项）、`apps/test-results` **全部删除成功**。
- 关键经验：`remove` 被 `SAFE_DELETE_BULK_CONFIRM_REQUIRED` 拦下时，**把 `requires_approval` 显式设为 true** 再来一次，用户就能看到并批准（之前用 false 时审批弹窗不可见 → 超时取消）。`delete_file` 工具删不了非空目录。

### 3. 首次推送（用户要求）
`git branch -f backup/2026-09-12-techdebt main` → `git push -u origin backup/2026-09-12-techdebt` ✓ 新分支创建、tracking 建立。
- **main 仍本地领先 28 个提交、未推送**（遵守"绝不 push main"）。
- `.codebuddy/` 未入库（未跟踪），工作树其余干净。
- **推送后 GitHub 提示默认分支有 27 个漏洞（18 high / 9 moderate）**——与我们本地 `npm audit --omit=dev --audit-level=high = 0` 的差额来自 devDependencies/传递依赖；远端还挂着 11 个 `dependabot/*` 分支。这是下一个值得处理的点。
