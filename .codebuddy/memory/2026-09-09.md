# 2026-09-09

## 全量代码 + 文档走查（不完善项盘点）
- 方式：4 个 code-explorer 子代理分域（server / web / desktop+打包 / agent+MCP+CLI）并行审查，主代理复核关键结论。
- 产物：向用户输出「不完善清单」，按 P0/P1/P2 + 文档与流程分组，附文件:行号。
- 复核确认的硬结论：
  - `image-proxy.ts:80-90` 只校验初始 hostname，`proxyImage` fetch 用 `redirect: "follow"`（:200）→ SSRF 可被 302 绕过；该模块无测试。
  - `db.ts:432` `migrateDatabase()` 无外层事务，~50 条 DDL 半途失败不可回滚。
  - `main.mts:54-57` 静态 import `desktop-smoke.mjs`；实测 2315 行。
  - 全仓无 `electron-updater` / `autoUpdater` 引用，自研 ZIP 更新。
  - `apps/desktop/package.json:12` test 列表 21 个，tests 目录 24 个 → 漏跑 `agent-host-lifecycle` / `agent-service-start` / `spawn-environment`。
  - 无 `App.test.tsx`；App.tsx 实测 3818 行。
- 在途未提交：25 文件 +708/-129（locale-boot、latest-first sync、renderer-cache-policy、startup log 等），尚未走交付流程。

## 安全收口首批（Batch S，已改代码，未提交）
1. `apps/server/src/image-proxy.ts` SSRF 加固：重写 `isAllowedUrl`（IPv6 去方括号——原 `h === "::1"` 因 hostname 返回 `[::1]` 从未命中、补 ULA/CGNAT/TEST-NET/组播、禁 URL 内凭据、限长）；改 `http/https` 手动逐跳（≤5）+ `nextRedirectUrl` 取代 `redirect:"follow"`；加 `guardedLookup` 解析期过滤私有地址防 rebinding。新增 `tests/image-proxy.test.ts` 14 条。
2. `apps/server/src/db.ts`：迁移 + schema_version 包进 `db.transaction`；失败先 `db.close()` 再抛（原泄漏连接 → Windows 锁文件，下次启动也失败，测试先撞到 EPERM）。新增 db.test.ts 回滚用例。
3. `apps/server/src/routes/messages.ts`：补 `preClose` abort `sentVerificationAbortController`。
4. `apps/desktop/package.json`：补挂 3 个漏跑测试；更正 `agent-service-start.test.ts` 2 条失效用例（launcher 命令现由 `cli-entry.mts` 分派，client 返回 NOT_SUPPORTED）。桌面 157 全绿。
5. `apps/desktop/src/main.mts`：新增 `runtime-log.jsonl` + `installDesktopRuntimeDiagnostics()`（uncaughtException/unhandledRejection/render-process-gone/child-process-gone + console.error|warn 镜像）。

## 结论修正 / 新发现
- 「两套加密格式一套缺 AAD」**是误报**：生产写入走 `encryptBoundSecret`/`credentialAad`（AAD 绑定），`decryptSecret` 仅读遗留 v1；跨账号搬运密文已有测试锚定。
- 新债：`apps/desktop/tsconfig.json` 只 include `src/**`，tests 不跑也不 typecheck（含 tests 时约 25 处真实类型错误）→ 是 3 个测试文件腐化的根因，留独立批次。
- server 全量 785 用例：`app.test.ts` 2 条失败系**在途 settings 默认值改动**的预存失败，非本批引入。
